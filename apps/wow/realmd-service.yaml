apiVersion: v1
kind: Service
metadata:
  name: wow-realmd
  namespace: wow
spec:
  type: ClusterIP
  selector:
    app: wow-realmd
  ports:
    - name: realm
      port: 3724
      targetPort: realm
      protocol: TCP
