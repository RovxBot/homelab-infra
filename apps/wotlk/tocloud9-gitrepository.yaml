apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: tocloud9
  namespace: flux-system
spec:
  interval: 12h
  ref:
    # Keep the Helm chart and its v0.1.0 service images in lockstep.
    tag: v0.1.0
  timeout: 2m
  url: https://github.com/walkline/ToCloud9.git
