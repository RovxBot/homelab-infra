apiVersion: batch/v1
kind: CronJob
metadata:
  name: skyfire-client-db2-extract
spec:
  # One-time recovery for an early extractor run which left db2 in its
  # ephemeral workspace. It runs mapextractor only; maps, vmaps, and mmaps are
  # left untouched.
  suspend: true
  schedule: "0 4 1 1 *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 604800
      activeDeadlineSeconds: 3600
      backoffLimit: 0
      template:
        spec:
          automountServiceAccountToken: false
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            fsGroup: 10001
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: extract-db2
              image: ghcr.io/rovxbot/skyfire-mop:core-21d5cb192b38c4675cc5c1aa6fc0b35e031501f6-cdbd4fe376ccfc070dfb4b9da83828a07b860eca@sha256:14c14720c7257b1ec2e6b4b89c0a780e2b9b67475f0cf5b681c97e3d959ce942
              imagePullPolicy: Always
              resources:
                requests:
                  cpu: "2"
                  memory: 4Gi
                limits:
                  cpu: "4"
                  memory: 8Gi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: [ALL]
                privileged: false
              command:
                - sh
                - -ec
                - |
                  client_data=""
                  for candidate in /client/Data /client/data; do
                    if [ -d "${candidate}" ]; then
                      client_data="${candidate}"
                      break
                    fi
                  done
                  if [ -z "${client_data}" ]; then
                    client_data="$(find /client -maxdepth 4 -type d \( -name Data -o -name data \) -print -quit || true)"
                  fi
                  test -n "${client_data}" && test -d "${client_data}" || {
                    echo 'Expected a WoW 5.4.8 client Data/ directory beneath /client.' >&2
                    exit 1
                  }
                  rm -rf /work/extract
                  mkdir -p /work/extract
                  ln -s "${client_data}" /work/extract/Data
                  cd /work/extract
                  /opt/skyfire/bin/mapextractor
                  db2_count="$(find db2 -maxdepth 1 -type f -name '*.db2' | wc -l)"
                  test "${db2_count}" -ge 21 || {
                    echo "Expected at least 21 DB2 files, found ${db2_count}." >&2
                    exit 1
                  }
                  rm -rf /data/db2
                  mv db2 /data/db2
              volumeMounts:
                - name: client
                  mountPath: /client
                  readOnly: true
                - name: data
                  mountPath: /data
                - name: work
                  mountPath: /work
          volumes:
            - name: client
              persistentVolumeClaim:
                claimName: skyfire-client-files
            - name: data
              persistentVolumeClaim:
                claimName: skyfire-data-rwx
            - name: work
              emptyDir: {}
