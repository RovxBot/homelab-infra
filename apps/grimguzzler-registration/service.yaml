apiVersion: v1
kind: Service
metadata:
  name: grimguzzler-registration
  namespace: wotlk
spec:
  selector:
    app: grimguzzler-registration
  ports:
    - name: http
      port: 80
      targetPort: http