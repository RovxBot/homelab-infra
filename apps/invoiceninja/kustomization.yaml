apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: invoiceninja
labels:
  - includeSelectors: false
    includeTemplates: true
    pairs:
      homelab.cooked.beer/owner: sam

resources:
  - namespace.yaml
  - pvc-data.yaml
  - mysql.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ../../secrets/invoiceninja.enc.yaml
