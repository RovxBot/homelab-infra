apiVersion: apps/v1
kind: Deployment
metadata:
  name: wireguard-client
  labels:
    app: wireguard-client
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: wireguard-client
  template:
    metadata:
      labels:
        app: wireguard-client
    spec:
      # Pin to a single node so we only have one tunnel + NAT gateway.
      nodeSelector:
        kubernetes.io/hostname: metal7
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      terminationGracePeriodSeconds: 10
      initContainers:
        - name: stage-wireguard-config
          image: busybox:1.37.0@sha256:1487d0af5f52b4ba31c7e465126ee2123fe3f2305d638e7827681e7cf6c83d5e
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          command:
            - sh
            - -lc
            - |
              install -d -m 700 /config/wg_confs
              install -m 600 /source/vps.conf /config/wg_confs/vps.conf
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 64Mi
          volumeMounts:
            - name: config
              mountPath: /config
            - name: wgconfs
              mountPath: /source
              readOnly: true
      volumes:
        - name: config
          emptyDir: {}
        - name: wgconfs
          secret:
            secretName: wireguard-client-conf
            defaultMode: 0440
            items:
              - key: wg0.conf
                path: vps.conf
      
      containers:
        - name: wireguard
          image: lscr.io/linuxserver/wireguard@sha256:33d861fd6ebcdc0f8d946ace22fa1efc2e3fc81951028bca2dea65f40fe05f93
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            capabilities:
              add:
                - NET_ADMIN
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Australia/Adelaide
          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -lc
                  - |
                    # Block pod-local traffic from reaching host WoW ports.
                    iptables -C OUTPUT -d "${HOST_IP}" -p tcp -m multiport --dports 3724,8085 -j REJECT 2>/dev/null || \
                    iptables -I OUTPUT 1 -d "${HOST_IP}" -p tcp -m multiport --dports 3724,8085 -j REJECT
          volumeMounts:
            - name: config
              mountPath: /config
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - "wg show vps >/dev/null 2>&1"
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            exec:
              command:
                - sh
                - -lc
                - "wg show vps >/dev/null 2>&1"
            initialDelaySeconds: 20
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
