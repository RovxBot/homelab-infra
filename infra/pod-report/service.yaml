apiVersion: v1
kind: Service
metadata:
  name: pod-report
  namespace: homepage
spec:
  type: ClusterIP
  selector:
    app: pod-report
  ports:
    - name: http
      port: 80
      targetPort: http
