apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - includeSelectors: false
    includeTemplates: true
    pairs:
      homelab.cooked.beer/owner: sam
resources:
  - namespace.yaml
  - deployment.yaml
  - pdb.yaml
  # Take ownership before the retired observability-config reconciler is
  # removed in the follow-up change.
  - cloudflared-metrics-service.yaml
  - ../../secrets/cloudflared-credentials.enc.yaml
configMapGenerator:
  - name: cloudflared-config
    namespace: cloudflared
    files:
      - config.yaml=config.yaml
