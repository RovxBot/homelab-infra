diff --git a/conf/playerbots.conf.dist b/conf/playerbots.conf.dist
index a03b13e3c..58453adff 100644
--- a/conf/playerbots.conf.dist
+++ b/conf/playerbots.conf.dist
@@ -2163,6 +2163,12 @@ AiPlayerbot.CommandServerPort = 8888
 AiPlayerbot.SpellDump = 0
 AiPlayerbot.LogInGroupOnly = 1
 AiPlayerbot.LogValuesPerTick = 0
+# Enable structured AQ40-only raid strategy logging without enabling generic
+# per-tick playerbot value dumps.
+AiPlayerbot.Aq40StrategyLog = 0
+# AQ40 throttling is applied per bot + event + state. Increase this to reduce
+# repeated raid spam, or lower it for more detail while debugging.
+AiPlayerbot.Aq40StrategyLogThrottleMs = 5000
 AiPlayerbot.RandomChangeMultiplier = 1
 
 # Tell which spell is avoiding (experimental)
diff --git a/src/Ai/Raid/Aq40/Util/RaidAq40Helpers_Shared.cpp b/src/Ai/Raid/Aq40/Util/RaidAq40Helpers_Shared.cpp
index 713eadbd4..da7a4743f 100644
--- a/src/Ai/Raid/Aq40/Util/RaidAq40Helpers_Shared.cpp
+++ b/src/Ai/Raid/Aq40/Util/RaidAq40Helpers_Shared.cpp
@@ -1,5 +1,6 @@
 #include "RaidAq40Helpers_Shared.h"
 
+#include <algorithm>
 #include <cctype>
 #include <mutex>
 #include <sstream>
@@ -89,13 +90,16 @@ bool IsAq40FollowRecoveryCandidate(Player* bot, PlayerbotAI* botAI)
 void LogAq40(Player* bot, std::string const& eventKey, std::string const& stateKey,
              std::string const& fields, uint32 throttleMs, bool warn)
 {
-    // Reuse the existing debug toggle instead of carrying extra AQ40-only config.
-    if (!sPlayerbotAIConfig.logValuesPerTick || !bot)
+    if (!sPlayerbotAIConfig.aq40StrategyLog || !bot)
         return;
 
     uint32 const instanceId = GetAq40LogInstanceId(bot);
     uint64 const botGuid = bot->GetGUID().GetRawValue();
-    uint32 const effectiveThrottleMs = throttleMs ? throttleMs : kAq40StrategyLogThrottleMs;
+    uint32 effectiveThrottleMs = throttleMs;
+    if (uint32 const configuredThrottleMs = sPlayerbotAIConfig.aq40StrategyLogThrottleMs)
+        effectiveThrottleMs = effectiveThrottleMs ? std::max(effectiveThrottleMs, configuredThrottleMs) : configuredThrottleMs;
+    else if (!effectiveThrottleMs)
+        effectiveThrottleMs = kAq40StrategyLogThrottleMs;
 
     std::ostringstream key;
     key << instanceId << ":" << botGuid << ":" << ToAq40LogToken(eventKey) << ":" << stateKey;
diff --git a/src/PlayerbotAIConfig.cpp b/src/PlayerbotAIConfig.cpp
index 37c74ac2a..8e5d7803c 100644
--- a/src/PlayerbotAIConfig.cpp
+++ b/src/PlayerbotAIConfig.cpp
@@ -381,6 +381,8 @@ bool PlayerbotAIConfig::Initialize()
         sConfigMgr->GetOption<int32>("AiPlayerbot.RandomBotAutoJoinBGRatedArena5v5Count", 0);
     logInGroupOnly = sConfigMgr->GetOption<bool>("AiPlayerbot.LogInGroupOnly", true);
     logValuesPerTick = sConfigMgr->GetOption<bool>("AiPlayerbot.LogValuesPerTick", false);
+    aq40StrategyLog = sConfigMgr->GetOption<bool>("AiPlayerbot.Aq40StrategyLog", false);
+    aq40StrategyLogThrottleMs = sConfigMgr->GetOption<uint32>("AiPlayerbot.Aq40StrategyLogThrottleMs", 5000);
     fleeingEnabled = sConfigMgr->GetOption<bool>("AiPlayerbot.FleeingEnabled", true);
     summonAtInnkeepersEnabled = sConfigMgr->GetOption<bool>("AiPlayerbot.SummonAtInnkeepersEnabled", true);
     randomBotMinLevel = sConfigMgr->GetOption<int32>("AiPlayerbot.RandomBotMinLevel", 1);
diff --git a/src/PlayerbotAIConfig.h b/src/PlayerbotAIConfig.h
index fa8a10dee..dfbcda613 100644
--- a/src/PlayerbotAIConfig.h
+++ b/src/PlayerbotAIConfig.h
@@ -249,7 +249,8 @@ public:
 
     uint32 randomBotTeleLowerLevel, randomBotTeleHigherLevel;
     std::map<uint32, std::pair<uint32, uint32>> zoneBrackets;
-    bool logInGroupOnly, logValuesPerTick;
+    bool logInGroupOnly, logValuesPerTick, aq40StrategyLog;
+    uint32 aq40StrategyLogThrottleMs;
     bool fleeingEnabled;
     bool summonAtInnkeepersEnabled;
     std::string combatStrategies, nonCombatStrategies;
