name: Workflow and YAML Lint

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/**"
      - ".yamllint.yml"

permissions:
  contents: read

concurrency:
  group: workflow-and-yaml-lint-${{ github.ref }}
  cancel-in-progress: true

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect workflow changes
        id: changes
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            git fetch --no-tags origin "$BASE_REF"
            range="$(git merge-base HEAD "origin/$BASE_REF")...HEAD"
          elif [[ -n "$BEFORE_SHA" && "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ]]; then
            range="$BEFORE_SHA...HEAD"
          else
            range="$(git rev-list --max-parents=0 HEAD | tail -n 1)...HEAD"
          fi

          changed_files="$(git diff --name-only "$range")"
          printf 'Changed files in %s:\n%s\n' "$range" "$changed_files"

          if echo "$changed_files" | grep -Eq '(^\.github/workflows/|^\.yamllint\.yml$)'; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip when workflow files are unchanged
        if: steps.changes.outputs.should_run != 'true'
        shell: bash
        run: echo "No workflow or yamllint config changes detected; actionlint is not needed."

      - name: Install shellcheck
        if: steps.changes.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Install actionlint
        if: steps.changes.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          ACTIONLINT_VERSION="1.7.8"
          curl -fsSL \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" \
            -o /tmp/actionlint.tgz
          tar -xzf /tmp/actionlint.tgz -C /tmp
          sudo mv /tmp/actionlint /usr/local/bin/actionlint
          actionlint -version

      - name: Lint GitHub Actions workflows
        if: steps.changes.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          actionlint

  yamllint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect workflow changes
        id: changes
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            git fetch --no-tags origin "$BASE_REF"
            range="$(git merge-base HEAD "origin/$BASE_REF")...HEAD"
          elif [[ -n "$BEFORE_SHA" && "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ]]; then
            range="$BEFORE_SHA...HEAD"
          else
            range="$(git rev-list --max-parents=0 HEAD | tail -n 1)...HEAD"
          fi

          changed_files="$(git diff --name-only "$range")"
          printf 'Changed files in %s:\n%s\n' "$range" "$changed_files"

          if echo "$changed_files" | grep -Eq '(^\.github/workflows/|^\.yamllint\.yml$)'; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip when workflow files are unchanged
        if: steps.changes.outputs.should_run != 'true'
        shell: bash
        run: echo "No workflow or yamllint config changes detected; yamllint is not needed."

      - name: Setup Python
        if: steps.changes.outputs.should_run == 'true'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.14"

      - name: Install yamllint
        if: steps.changes.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --quiet yamllint

      - name: Lint YAML
        if: steps.changes.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          yamllint -c .yamllint.yml .github/workflows
